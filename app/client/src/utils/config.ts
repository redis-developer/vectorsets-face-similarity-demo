const API_BASE_URL = `${import.meta.env.BASE_URL}api`;

const IMAGE_BASE_URL = '';
const MAX_UPLOAD_FILE_SIZE = 5 * 1024 * 1024; // 5MB

export { API_BASE_URL, IMAGE_BASE_URL, MAX_UPLOAD_FILE_SIZE };
